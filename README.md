# net-shield-recon
A defensive Python security tool that parses authentication logs using regex to detect brute-force signatures and automates active threat containment via live iptables firewall rules.
